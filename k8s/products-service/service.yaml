apiVersion: v1
kind: Service
metadata:
  name: products-service
  labels:
    app: products
spec:
  type: NodePort
  selector:
    app: products
  ports:
  - protocol: TCP
    port: 3002
    targetPort: 3002
    nodePort: 30002
